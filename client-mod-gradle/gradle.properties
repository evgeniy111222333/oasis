# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# IntelliJ IDEA is not yet fully compatible with configuration cache, see: https://github.com/FabricMC/fabric-loom/issues/1349
org.gradle.configuration-cache=false

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=26.1.2
loader_version=0.19.3
loom_version=1.17-SNAPSHOT
# Fabric has not published Yarn mappings for 26.1.2 yet. Keep this explicit so the runtime target and compile mappings are not confused.
yarn_mappings_version=1.21.2+build.1

# Mod Properties
mod_version=1.2.3
maven_group=ua.rp.chat

# Dependencies
fabric_api_version=0.153.0+26.1.2
